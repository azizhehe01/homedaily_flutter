import 'package:flutter/material.dart';

class ViewAllProdukPage extends StatelessWidget {
  const ViewAllProdukPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('View All Produk')),
      body: const Center(child: Text('View All Produk Page Content')),
    );
  }
}
